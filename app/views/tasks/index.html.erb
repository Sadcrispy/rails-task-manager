<div class="container">
  <h1>🇵🇱 My 🇵🇱 Tasks 🇵🇱</h1>
  <ul class="container list-unstyled">
    <% @tasks.each do |task| %>
      <% if task.completed %>
        <li class="mt-3"><i class="fa-regular fa-square-check"></i> <%= link_to task.title, task_path(task), class: "text-decoration-none text-danger" %></li>
      <% else %>
        <li class="mt-3"><i class="fa-regular fa-square"></i> <%= link_to task.title, task_path(task), class: "text-decoration-none text-danger" %></li>
      <% end %>
    <% end %>
  </ul>
  <div class="container">
    <%= link_to "ADD A TASK", new_path, class: "text-decoration-none text-danger" %>
  </div>
</div>
